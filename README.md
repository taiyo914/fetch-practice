# fetch-practice

**`git fetch`** は、GitHubなどの**リモートリポジトリから最新のデータを、ローカルリポジトリにダウンロードする**コマンドです。

このコマンドを実行しても、あなたが今作業しているファイル（ワークツリー）の内容は**一切変更されません。** あくまで「リモートでは今どうなっているか」という情報を手元のデータベースに取り込むだけの安全な操作です。
